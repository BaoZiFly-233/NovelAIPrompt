feat: 五大 ViewModel 全面解耦至 Repository 抽象，接入 Coil 位图池与崩溃日志回传 (评审 P2 落地)

上层模块全部从直接依赖 NovelAIApiService/ImageDao 切换为仅依赖 core:data 的 ImageRepository 与
GenerationRepository：workbench/gallery/compare/swipe/inpaint 五个 ViewModel 的统一改造消灭了
SRP 与 DIP 违规，工作台生成流程改为「编辑→仓储双轨调度→落盘入库→Outcome 回写 UI」，图库网格
改用 WebP 缩略图加载并透传收藏/归档/回填操作。composeApp 侧同步落地：appModule 注册
ImageFileStorage/ImageRepository/GenerationRepository 单例并修复 DataStore 把目录当文件读的隐患
（settings 改为完整文件路径）；Android 端 StudioApplication 实现 SingletonImageLoader.Factory 配置
Coil 3 全局单例（内存缓存取堆 25%、磁盘缓存落 cacheDir 上限 256MB）；新增双端 CrashReporter 把未捕获
异常落盘、设置页展示上次崩溃堆栈，无需 USB 调试即可回溯闪退；另有 ViewModel 单元测试与仓储层测试
补齐（runTest+MainDispatcher 假实现），全量 jvmTest 与 androidApp-debug.apk 构建通过。

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>